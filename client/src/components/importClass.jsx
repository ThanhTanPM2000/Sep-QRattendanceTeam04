import React from "react";

import ModalImport from "./common/modalImport";

const ImportClass = ({ show, onHide }) => {
	

  return <ModalImport show={show} onHide={onHide} />;
};

export default ImportClass;
